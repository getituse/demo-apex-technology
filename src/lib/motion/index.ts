export { MotionProvider } from "./MotionProvider";
export * from "./variants";

// `m` is the tree-shakeable element factory that LazyMotion requires.
export { AnimatePresence, m, useReducedMotion } from "motion/react";
